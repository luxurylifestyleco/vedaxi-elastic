# Security Baseline

Generated by `scripts/security_baseline.py` on 2026-09-10 15:02 UTC.
Repository: `luxurylifestyleco/vedaxi-elastic` (public foundation). This file is a **historical** scan snapshot, not a live attestation.

This is a **baseline** static scan, not an enterprise security suite.
It uses pattern matching and reports findings for human triage; it does
not prove exploitability. Run a real scanner (pip-audit, bandit, gitleaks,
Semgrep) for production assurance.

## Summary

- **Total findings:** 3
- **Secret scanning:** 2
- **Dependency vulnerabilities:** 0
- **Unsafe deserialization:** 0
- **Input validation (eval/exec):** 0
- **Command injection:** 0
- **Path traversal:** 0
- **SQL injection:** 1

## Secret scanning

| File | Line | Check | Snippet |
|------|------|-------|---------|
| `C:\Users\m_jor\Documents\elastic-web\scripts\db-health.sh` | 17 | Generic password assignment | `PGPASSWORD="${PGPASSWORD:-postgres}"` |
| `C:\Users\m_jor\Documents\elastic-web\scripts\db-init.sh` | 32 | Generic password assignment | `PGPASSWORD="${PGPASSWORD:-postgres}"` |

## SQL injection

| File | Line | Check | Snippet |
|------|------|-------|---------|
| `C:\Users\m_jor\Documents\elastic-web\scripts\pgvector_benchmark.py` | 208 | f-string SQL | `f"INSERT INTO bench_capabilities (name, description, category, embedding) "` |

## Remediation notes

- **Secrets:** rotate any real credential immediately; move to env vars /
  a secret manager; never commit `.env` or key files.
- **Unsafe deserialization:** prefer `json.loads`; never `pickle`/`yaml.load`
  on untrusted input.
- **eval/exec:** replace with safe, whitelisted logic.
- **Command injection:** avoid `shell=True`; pass argument lists to
  `subprocess.run`.
- **Path traversal:** validate/normalize user-supplied paths against a
  trusted root before `open`/`os.path.join`.
- **SQL injection:** use parameterized queries (psycopg2 `%(name)s`), never
  f-string/`%`/`.format` interpolation of user input into SQL.
- **Dependency vulns:** install `pip-audit` and run it in CI.
